# Slim inference deps for the free-tier image (Dockerfile.lite).
# Serves the .tflite model with a lightweight LiteRT runtime — NO TensorFlow —
# so the container fits in ~512 MB RAM (low-RAM / 512 MB-class hosts).
# The full-TF inference side stays in app/requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
ai-edge-litert>=1.0
Pillow>=10.0
numpy>=1.24
